.view-wrapper( ng-init="setHeading('Compose Note')")
  .content-container
      search-panel
        #sub-options-holder(ng-controller="composerCtrl")
          .sub-option
            //- p Title
            input(type="text" ng-model='submitTitle' placeholder="Title")
          .sub-option
            //- p Description
            input(type="text" ng-model='submitDesc' placeholder="Description")
          .sub-option
            //- p Subject
            input(list="subjects" ng-model="submitSubject" placeholder="Subject")
            datalist(id="subjects")
                option(ng-repeat="subject in subjects" value="{{subject}}")

          .sub-option
            //- p Tags
            input(type="text" ng-model="submitTags" placeholder="Tags (comma deliminated,no hastags)")
          .sub-option
            #editButton Write
            #submitButton(ng-click="submitNote()") Send
                  //- span(class="glyphicon glyphicon-send")
      .content-panel(show="false")
          i#expand-icon(class="fa fa-arrows-alt")
          i#close-icon(class="fa fa-send")
          i#shrink-submit(class="fa fa-expand")
          #editor-toolbar-holder
            text-angular-toolbar( name="toolbar" id='toolbar')
          #editor-background-fill
          div(ta-text-editor-class="form-content"
          ta-html-editor-class="form-content"
          ta-focussed-class="focussed"
          text-angular text-angular-name="editor"
          id="editor"
          name="editor"
          ng-model="holder.htmlContent"
          ta-target-toolbars='toolbar'
          on='false'
          )
         
//textarea(  id="from" style="width: 100%;display:none;" ng-bind-html="holder.trusted")
//- textarea(  id="from" style="width: 100%;display:none;"){{$parent.notes[0].content}}
